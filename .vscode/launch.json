{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Chrome with Live Server (port 5500)",
        "type": "chrome",
        "request": "launch",
        "url": "http://localhost:5500",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  
